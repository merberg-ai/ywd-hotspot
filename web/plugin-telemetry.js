'use strict';
// Compatibility shim: the retired MMDVM Live Telemetry plugin UI was removed
// in Alpha22.7.3. Core telemetry remains available to trusted YWD runtime
// consumers; this file stays temporarily so older cached app.js loaders do not
// produce a 404 while browsers refresh across the update.
